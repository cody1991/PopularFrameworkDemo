define(function(require, exports, module) {
 /*
    var $ = require("lib/zepto/zepto"), activityID = $("body").data("app-id"), statistics = {behaviorStatistics: function(plugType) {
             $.ajax({url: "/analyseplugin/plugin?activity_id=" + activityID + "&plugtype=" + plugType,type: "get"})
         },multiStatistics: function() {
             var channel_id = location.search.substr(location.search.indexOf("channel=") + 8);
             channel_id = channel_id.match(/^\d+/), (!channel_id || isNaN(channel_id) || 0 > channel_id) && (channel_id = 1), $.ajax({type: "POST",cache: !1,url: "/brand/analyse/" + activityID + "?channel=" + channel_id,data: {channel: channel_id},dataType: "json",success: function(msg) {
                     msg.result
                 }})
         }};
     setTimeout(function() {
         statistics.multiStatistics()
     }, 2e3), module.exports = statistics*/
 
});
